# 2026-09-08

## 平台级改造：登录方式 + 多租户 + RBAC + PostgreSQL

### 1. 登录方式：令牌 → 账号密码
- 后端新增 `internal/auth`：bcrypt 密码、JWT（access + refresh 双令牌，refresh 存库可吊销）。
- 接口：`POST /api/v1/auth/login|refresh|logout`、`GET /auth/profile`、`PUT /auth/password`。
- 鉴权从 `X-Admin-Token` 改为 `Authorization: Bearer <jwt>`；WS 走 `?token=` 查询参数。
- 前端 `web/src/api/index.js`：401 自动续期一次，失败清会话跳登录；`AppLayout` 顶栏改为用户菜单（改密码/退出）。

### 2. 多租户 + RBAC
- 数据隔离粒度（用户确认）：**业务数据按租户隔离，模型配置（Provider/LLMModel）全局共享**。
- 新增实体：Tenant / User / Role / RolePermission / UserRole / AuthToken；业务表（Project/Repo/Credential/Rule/Event/Task/IngestToken/ReviewJob/CredentialUsage）加 `tenant_id`。
- 权限码 `资源:动作` 目录在 `model/rbac.go`；内置角色：super_admin(平台)、tenant_admin/developer/viewer(每租户)。
- 超管可跨租户（`X-Tenant-ID` 头切换），非超管强制本租户过滤（api 层 `h.tdb(c)` 统一加 `tenant_id`）。
- 前端新增 用户管理/角色权限/租户管理 三个页面；路由守卫按 `meta.perm` 过滤菜单与访问。

### 3. PostgreSQL
- `store/db.go` 新增 postgres 驱动（gorm.io/driver/postgres）；`Dialect` 全局变量。
- SQL 方言适配 `api/dialect.go`：`sum(case when…)` / `to_char` / `::text` / GROUP BY 列补全。
- 目标库：`new.hsddns.com:5432` 账号 admin，库 automedic（TEMPLATE template0 创建，避开 collation 版本不匹配）。
- 配置：`configs/config.yaml` 已切 postgres；`auth.jwt_secret` + `auth.bootstrap_admin`（admin/admin123，默认租户 default）。
- 种子：`store/seed_rbac.go` 幂等创建租户/角色/权限/超管；`SeedDefault` 继续种厂家与模型。

### 验证
- 端到端 curl：登录 → 建租户 → 建项目，租户隔离正确（tenant_id=1/2 各见其项目）；
  bob（租户B管理员）跨租户/改模型配置均 403；viewer 建项目 403、列表 200。
- `go vet` 干净；`go test ./...` 全绿（git/ocr/service 通过）。
- 前端 `vite build` 通过；playwright 截图登录页/用户/角色/租户页正常。

### 遗留
- 未做：SQLite 数据迁移到 PG（用户选择全新初始化）；旧 admin_token 字段保留但已废弃。
- `server/configs/config.docker.yaml` 与 `docker-compose.yml` 仍指向 mysql，Docker 部署需另切 postgres（未改）。

## 全项目深度 Code Review（输出 `docs/代码审查报告-2026-09-08.md`）
验证基线：`go build`/`go vet`/`gofmt` 干净，`go test ./...` 全绿，`-race` 无告警。
已确认的高危项（待修，勿重复排查）：
- **P0**：`configs/config.yaml` 生产 DSN 明文口令入库；Rule 接口（`api/llm.go:479+`）无租户过滤且 CreateRule 不写 tenant_id；`git/workspace.go:82` 工作区路径不含 taskID → 并发任务互踩；`api/rbac.go` CreateUser 接受 `is_super` + SetRolePermissions 不校验权限码白名单 → tenant_admin 可提权；Web 可改 dsh `command_template` 与项目 `release_hook` → `/bin/sh -c` RCE；HTTP 凭据拼进 git URL 会进 task_logs 与 slog。
- **P1**：`api/task.go:79` TaskLogs 无租户校验；`ws/hub.go:92` unsubscribe 重复 close channel 会 panic；`execx/execx.go:52` 设了 Setpgid 但没有杀进程组逻辑 → 超时后子进程残留、Wait 可能永久阻塞；`api/handlers.go:118` UpdateSettings 直接写共享 config 指针（竞争 + 注释说落库实际没有）；任务抢占非原子（scanner 重复入队）；Confirm 路径未 CleanupArtifacts 且 Patch 在清理前生成 → AUTOMEDIC.md 会被提交；登录无限流；CORS `*` + credentials；gin 未设 TrustedProxies → XFF 可伪造绕过 ingest IP 白名单；JWT 密钥兜底硬编码常量；容器 root 运行。
- 前端（并行审查）：令牌存 localStorage；401 拦截器 refresh 失败会递归；无 overview:read 权限登录死循环白屏；WS token 走 query。
- 测试空白：auth / config / crypto / dsh / execx / ws 无单测，越权无回归用例。

## P0–P2 修复（同一天，未 commit，改动约 43 文件）
- 已修：Rule 四接口租户过滤 + CreateRule 写 tenant_id；TaskLogs/ConfirmTask/RejectTask/GetReviewJob/FixReviewJob 归属校验；CreateUser 的 is_super 仅超管可设；SetRolePermissions 权限码白名单；JWT 密钥兜底改随机（rand 失败 panic，不再回落硬编码）+ 支持 secret_key_file；CORS 源走 server.allow_origins（未配置=关闭跨域）；新增 server.trusted_proxies（空=不信任代理）；ws once.Do 防重复 close；execx Kill(-pgid) 进程组回收；任务原子抢占（RowsAffected）；Executor 按 repoID 串行锁；Confirm 前 CleanupArtifacts 且 Patch 移到清理后；UpdateSettings 加 settingsMu；git.runOut 统一 ScrubURL；CredentialUsage 补 tenant_id；容器 appuser 非 root；logging 按天轮转 + retain 清理；config.yaml 去敏（真实值迁到 gitignore 的 config.local.yaml，dev.sh 优先加载）；.env.* 忽略 + git rm --cached。
- 前端：token 移出 localStorage（内存 + bootstrapSession 静默续期 + index.html 占位防白屏）；401 拦截器按 url 拦断 refresh 递归；无权限跳第一个有权限路由否则 /403(新增 Forbidden.vue)；WS 用 `['automedic.<token>','automedic']` 双协议（后端 upgrader Subprotocols=["automedic"]，只发带 token 那个会握手失败）；筛选改绑 search() 重置 page；15 处 MessageBox catch；TaskDetail 定时器清理；Dashboard setOption(...,true)。
- 新增后端行为：刷新令牌 30s 轮换宽限（markRefresh 只在 !Revoked 时调用，重放不续期窗口，否则旧令牌可无限复用）。
- 坑：gin-cors v1.7.2 在 AllowOrigins 为空且无 AllowOriginFunc 时直接 panic("all origins disabled")，要用恒 false 的 AllowOriginFunc 表达"关闭跨域"。
- 验证：go build/vet/gofmt 干净，go test -race ./... 全绿，vite build 通过。
- 待办（未做）：P0-6 command_template/release_hook 的 Web 写入仍是 RCE 面（需产品决策）；登录限流；生产库口令轮换 + git 历史清理；auth/dsh/ws 单测。
