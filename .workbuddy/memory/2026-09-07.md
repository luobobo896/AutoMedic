# 2026-09-07

## AutoMedic 收尾：缺陷修复 + 全链路验证 + 源码入库

### 修复：git 子进程环境变量未透传（重要）
- 现象：`git.Manager.runOut` 签名收了 `env` 却直接调 `execx.RunSimple` 丢弃，导致
  `GIT_SSH_COMMAND` / `GIT_ASKPASS` / `GIT_AUTHOR_*` 全部失效 —— SSH 密钥凭证 clone/push 静默失败，
  提交者身份回落到本机 git config。
- 修复：execx 新增 `RunSimpleEnv(ctx, dir, env, bin, args...)`（同时加 `Setpgid`）；`runOut` 透传 env；
  `Workspace` 新增 `authEnv` 字段 + `AuthEnv()`，`Push(ctx, url, env)` 复用创建时的凭证环境；
  `service.finalize` 取 `PublicAuthURL` 返回的 env 传给 Push。
- 回归用例：`server/internal/git/workspace_env_test.go`（假 ssh 脚本写 marker 验证 env 真到子进程）。

### 端到端测试（假 dsh，不调用大模型）
`server/internal/service/e2e_fix_test.go`，用 shell 脚本冒充 `dsh --profile headless`：
改代码 + 写 `.automedic/result.json` + 输出 `===AUTOMEDIC_RESULT===`。
覆盖 5 个用例：全自动提交/推送/发布钩子（含远端分支内容校验、指令文件未误提交）、
半自动 confirming→Confirm→推送、Reject 不产生 commit、no_code_change→ignored、工作区复用基线正确。
`./scripts/test.sh` = go vet + go test ./...，全部通过。

### 验证结论
- **MySQL 迁移**：docker 起 mysql:8.0.46，001/002 脚本执行无错；表名 `providers` / `rules`（不是 llm_providers / fix_rules），与 GORM 一致；
  服务以 `AUTOMEDIC_DB_DRIVER=mysql` 启动正常，healthz 200。
- **Docker 镜像**：实际 build 成功（约 2 分钟），容器 healthz/SPA 均 200。
- **dsh 官方 npm 包名是 `@deepseek-ai/dsh`**（scope 带 -ai，当前 latest 0.1.2-rc.1）。
  原 Dockerfile 写的 `@deepseek/dsh` / `dsh` 都装不上（且有误装恶意包风险），已更正。
- 新增 `deploy/docker-entrypoint.sh`：未提供 `AUTOMEDIC_SECURITY_SECRET_KEY` 时在 `/app/data/.secret_key`
  生成随机密钥并长期复用（0600）；`.dockerignore` 原先把 `deploy` 整个排除了，已放开。
  compose 里 `AUTOMEDIC_SECRET_KEY` 默认改为空，避免所有部署共用同一个默认密钥。

### 源码入库
仓库此前只有 1 个 README 提交，全部源码未入库。已提交 `c99776f`（75 文件 / +11812）。
**已 push 到 origin/main**（2026-09-07）：`c99776fe708ad48c7dd60998e69e6a72156abacd`。

### 真实 dsh 改代码链路（2026-09-07 补验）
隔离服务 `:18080` + 含空指针的本地 bare 仓库。
- 业务拒绝事件 → `ignored`（无规则命中）
- 空指针事件 → 规则命中 → 真实 `dsh --profile headless`（deepseek-v4-flash）
- 任务 `#1` `success`（105553ms）：`Level()` nil 保护 + `service_test.go`
- 提交 `efeec2ff` 作者 AutoMedic，仅 `service.go` / `service_test.go`；`AUTOMEDIC.md`/`AGENTS.md` 未入库
- 已推送分支 `automedic/fix-1-20260907-162108`；`GO111MODULE=off go test` PASS

### 环境备忘
- 本机 docker（OrbStack）可用；mysql client 在 `/opt/homebrew/opt/mysql-client/bin/mysql`，无 mysqld。
- 后台跑服务验证时用 `env A=1 B=2 nohup ./bin/...`，zsh 下 `(A=1 cmd &)` 形式会把赋值当成命令执行。

## ModelConfig 输出长度 384K 档位 + UI 视觉改造（2026-09-07 晚）

### 功能：输出长度下拉新增 384K（393216 tokens）
- `web/src/views/ModelConfig.vue` 重写：新增 `MODEL_SPECS` 官方规格表（slug 正则 → maxOutput/maxInput/note），
  按模型官方最大输出过滤下拉档位；384K 仅对 DeepSeek V4 系列（官方明示 384K）与 Kimi K3（1M 上限覆盖，within384 兼容档 + warning 提示）开放。
- 档位池 `LEN_POOL` 统一为「K（tokens）」标注，1K=1024；128000 与 131072 并存（OpenAI 十进制 vs 二进制）。
- watch 守护：slug/kind 变化时若当前值超官方上限自动回退。
- 未登记模型按 provider kind 给保守上限，未匹配则隐藏 384K 但保留 allow-create 手工输入。

### UI：参考用户截图（暗色卡片风）+ 微软/Apple 柔和风
- `web/src/styles/index.css` 主题重做：中性暗色（#161618/#1f1f22）、accent 柔和蓝 #6aa1ff、圆角 10-16px、
  Element Plus 全量变量柔和化（输入框 inset 底、focus 光晕、下拉浮层圆角、按钮 pill 化）。
- ModelConfig 页面布局改版：厂家卡片网格（点击选中蓝边高亮+当前 pill）、模型设置行、对话框分区（上下文设置 + 高级设置折叠）。
- 验证：vite build 通过；起 go server + vite dev，playwright 截图验证（/tmp/shot-1-page.png、shot-5-output-384k.png）。
  deepseek-v4-flash 输出下拉实际渲染 384K~8K 共 9 档，512K/1M 被正确过滤。
- 坑：前端路由是 hash 模式（/#/models）；截图需 localStorage automedic_admin_token=change-me 后 reload。
  playwright-cli 插件缺依赖跑不起来，用隔离 node workspace 装 playwright-core + ms-playwright 缓存的 Chrome for Testing。
